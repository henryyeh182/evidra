# Handoff — 2026-08-10

## Scope

本次工作涵蓋兩個 repository：

- `henryyeh182/fitness-mcp`
- `henryyeh182/evidra`

## Completed

### README and public documentation

- 重新整理兩個 repository 的 `README.md`，統一產品定位、決策模型、MCP tools、Evidence 規則、部署狀態、隱私說明與版本資訊。
- `fitness-mcp` README 保留 source repository 專用的開發指令與文件索引。
- 更新 `evidra/PRIVACY.md`，移除口語化敘述，明確定義 local desktop extension 的資料處理、保留、分享與未來 hosted deployment 邊界。
- `LICENSE` 維持原法律條款，未以技術文件方式改寫。

### Git history and remote state

`fitness-mcp`：

- README documentation commit：`7d717fe` — `Align README with public documentation`
- 已推送至 `origin/main`
- `main` 與 `origin/main` 一致
- 遠端已刪除並清理已合併的 `agent/update-pacevera-product-docs`

`evidra`：

- README and privacy commit：`feaf821` — `Align public README and privacy documentation`
- 已推送至 `origin/main`
- `main` 與 `origin/main` 一致
- 已刪除 3 個已合併的遠端分支：
  - `claude/install-verify-and-demo-prompts`
  - `claude/match-readme-to-shipped-version`
  - `claude/trim-readme`
- 目前遠端只保留 `main`。

## Verification

- 兩個 repository 的文件均通過 `git diff --check`。
- `fitness-mcp` 的 `npm test` 共 487 個測試：482 個通過，5 個 HTTP 整合測試因目前 sandbox 禁止 localhost `listen` 而失敗。失敗原因為執行環境權限，不是文件修改造成。
- 建立本交接檔前，兩個 repository 的工作樹均為乾淨狀態；目前 `fitness-mcp` 只有本檔案尚未提交，`evidra` 工作樹保持乾淨。

### R0 Rule Package boundary closeout

- `base_rules` manifest 現由正式的 repository JSON Schema 驗證；validator 也檢查 engine compatibility、Rule ID、content file path、checksum 與 released package 的 `reviewRecord`。
- `reviewRecord` 已定義為 package-local 的 `reviews/RR-YYYY-NNNN.md` 路徑，並新增 `RR-2026-0001.md` 記錄 R0；`running_rules` 與 `strength_rules` 明確維持空的 draft package。
- Runtime 已從 `rule-packages/base_rules` 載入 Rule Library；新增 package-to-runtime、MCP decision output／`decisionBasis`／`decisionId`／`explain_decision` 與 Decision Harness integration tests。
- 驗證結果：rules／R1 package tests 全綠、完整 test suite 492 tests／487 pass；5 個 localhost HTTP tests 因 sandbox 禁止 `listen` 而失敗。Harness 37/37 scenarios、eval 20/20 golden cases、`review:phase` G0–G11 全綠。
- G1 已把上述 sandbox HTTP failures 視為環境 baseline；G7 已明確隔離既有 local athlete state store 與 package manager local store，避免把合法本機持久化誤判為 LLM 或 outbound data sink。
- 下一個開發工作：R2 Evidence uplift。

### R1 Rule Package update and review closeout

- 新增 `scripts/rule-package.js` 與 `scripts/run-harness-json.js`，支援 package directory、tar／tar.gz 與 `.mcpb` 匯入。
- `validate` 會檢查 manifest、checksum、engine compatibility 與 released review record；`dry-run` 會以候選 package 子程序跑完整 37 個 Decision Harness scenarios，輸出 decision／action／decisionBasis／governing rule／confidence diff。
- `install` 必須明確帶 `--confirm`，只在 Harness 成功後把 package 複製到 immutable `packageId/version` 目錄，並更新本機 `active.json`；失敗驗證、checksum、相容性或 Harness 不會切換 pointer。
- `rollback` 由 `active-history.json` 選取前一個已驗證版本，並再次驗證 checksum 與 engine compatibility。
- R1 tests 覆蓋 immutable install、active pointer、rollback，以及失敗 candidate 不改變 pointer。下一個開發工作改為 R2 Evidence uplift。

## Current public documentation baseline

- 公開產品名稱：Pacevera
- Current release：`v0.4.2`
- Local desktop extension：available
- User-controlled private deployment：planned
- Hosted remote deployment：not available / no-go
- Evidence 由 calling AI host 提供；Pacevera 不代替使用者連接 Apple Health、Garmin、Strava、Oura 或 WHOOP。
- Pacevera 的決策計算為 deterministic；不呼叫模型產生決策。

## Follow-up

- 未來若產品部署模式、版本、工具清單或隱私承諾變更，需同步更新兩個 repository 的 README。
- 若 hosted remote deployment 對外開放，必須先更新 `evidra/PRIVACY.md`，並完成 authorization、retention、logging、deletion 與 infrastructure-level privacy review。
- 若需要在 GitHub 上建立新的工作分支，完成 merge 後應刪除已合併的本地與遠端分支。

### R2 Evidence uplift closeout

- `base_rules@1.1.0` 新增正式 schema 驗證的 5 個 evidence packets：HRV 導引訓練（EP-001）、阻力訓練進階（EP-002）、ACWR（EP-003）、detraining（EP-004）、急性睡眠不足（EP-005）。每個 packet 都記錄 claim、population、exposure、outcome、limitations、source type、PMID／DOI、retrieved date、applicable Rule IDs、reviewer 與 verification status。
- R-002 的舊未驗證 HRV 引用已替換為 PubMed abstract-checked 引用；R-006／R-007 的 evidence `studyDesign` 提升為 `systematic_review`，但 recommendation strength 仍是 `supports_direction_only`。
- 沒有把文獻誤解為 Pacevera 的 readiness／fatigue composite、ACWR 1.4、detraining 14／25／42／60 或 return factor 0.6 的驗證；Rule ID、rule count、decision output、decisionBasis、decisionId 與 `explain_decision` contract 維持不變。
- Review record：`rule-packages/base_rules/reviews/RR-2026-0002.md`。下一步是 R3 regression gate。

### R3 Regression gate closeout

- 新增 `harness/schemas/regression-baseline.schema.json` 與 `harness/regression-baseline.json`，固定 37 個 Harness golden／boundary cases；artifact 明確標示 `groundTruth: behavioral_regression_baseline`，不冒充醫學 ground truth。
- `npm run regression:gate` 驗證 baseline schema、case 數量與 current structured decision／Decision Graph diff。
- `npm run package:dry-run` 現在同時比較 active package、candidate package 與 regression baseline；case 增刪、decision、action、confidence、governing rule、coverage、limit 或 graph node／edge 變更都會 fail。
- R3 驗證結果：baseline pass、candidate dry-run pass、37 cases、decision diff 0、graph diff 0。下一步是 R4 graph viewer。
- R4 後完整 suite 為 497 tests／492 pass；5 個 localhost HTTP tests 仍是 sandbox `listen EPERM` baseline。`review:phase` 的 G1 已同步為 492。
